---
import Layout from '../../layouts/Layout.astro';
import EditorApp from '../../components/editor/Editor.tsx';

export const prerender = false;

const { id } = Astro.params;
---

<Layout>
	<EditorApp client:load projectId={id || 'default'} />
</Layout>
